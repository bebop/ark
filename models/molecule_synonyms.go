// Code generated by SQLBoiler 4.11.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MoleculeSynonym is an object representing the database table.
type MoleculeSynonym struct {
	Molregno  int64       `boil:"molregno" json:"molregno" toml:"molregno" yaml:"molregno"`
	SynType   string      `boil:"syn_type" json:"syn_type" toml:"syn_type" yaml:"syn_type"`
	MolsynID  int64       `boil:"molsyn_id" json:"molsyn_id" toml:"molsyn_id" yaml:"molsyn_id"`
	ResStemID null.Int64  `boil:"res_stem_id" json:"res_stem_id,omitempty" toml:"res_stem_id" yaml:"res_stem_id,omitempty"`
	Synonyms  null.String `boil:"synonyms" json:"synonyms,omitempty" toml:"synonyms" yaml:"synonyms,omitempty"`

	R *moleculeSynonymR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L moleculeSynonymL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MoleculeSynonymColumns = struct {
	Molregno  string
	SynType   string
	MolsynID  string
	ResStemID string
	Synonyms  string
}{
	Molregno:  "molregno",
	SynType:   "syn_type",
	MolsynID:  "molsyn_id",
	ResStemID: "res_stem_id",
	Synonyms:  "synonyms",
}

var MoleculeSynonymTableColumns = struct {
	Molregno  string
	SynType   string
	MolsynID  string
	ResStemID string
	Synonyms  string
}{
	Molregno:  "molecule_synonyms.molregno",
	SynType:   "molecule_synonyms.syn_type",
	MolsynID:  "molecule_synonyms.molsyn_id",
	ResStemID: "molecule_synonyms.res_stem_id",
	Synonyms:  "molecule_synonyms.synonyms",
}

// Generated where

var MoleculeSynonymWhere = struct {
	Molregno  whereHelperint64
	SynType   whereHelperstring
	MolsynID  whereHelperint64
	ResStemID whereHelpernull_Int64
	Synonyms  whereHelpernull_String
}{
	Molregno:  whereHelperint64{field: "\"molecule_synonyms\".\"molregno\""},
	SynType:   whereHelperstring{field: "\"molecule_synonyms\".\"syn_type\""},
	MolsynID:  whereHelperint64{field: "\"molecule_synonyms\".\"molsyn_id\""},
	ResStemID: whereHelpernull_Int64{field: "\"molecule_synonyms\".\"res_stem_id\""},
	Synonyms:  whereHelpernull_String{field: "\"molecule_synonyms\".\"synonyms\""},
}

// MoleculeSynonymRels is where relationship names are stored.
var MoleculeSynonymRels = struct {
	ResStem                    string
	MolregnoMoleculeDictionary string
}{
	ResStem:                    "ResStem",
	MolregnoMoleculeDictionary: "MolregnoMoleculeDictionary",
}

// moleculeSynonymR is where relationships are stored.
type moleculeSynonymR struct {
	ResStem                    *ResearchStem       `boil:"ResStem" json:"ResStem" toml:"ResStem" yaml:"ResStem"`
	MolregnoMoleculeDictionary *MoleculeDictionary `boil:"MolregnoMoleculeDictionary" json:"MolregnoMoleculeDictionary" toml:"MolregnoMoleculeDictionary" yaml:"MolregnoMoleculeDictionary"`
}

// NewStruct creates a new relationship struct
func (*moleculeSynonymR) NewStruct() *moleculeSynonymR {
	return &moleculeSynonymR{}
}

func (r *moleculeSynonymR) GetResStem() *ResearchStem {
	if r == nil {
		return nil
	}
	return r.ResStem
}

func (r *moleculeSynonymR) GetMolregnoMoleculeDictionary() *MoleculeDictionary {
	if r == nil {
		return nil
	}
	return r.MolregnoMoleculeDictionary
}

// moleculeSynonymL is where Load methods for each relationship are stored.
type moleculeSynonymL struct{}

var (
	moleculeSynonymAllColumns            = []string{"molregno", "syn_type", "molsyn_id", "res_stem_id", "synonyms"}
	moleculeSynonymColumnsWithoutDefault = []string{"molregno", "syn_type", "molsyn_id"}
	moleculeSynonymColumnsWithDefault    = []string{"res_stem_id", "synonyms"}
	moleculeSynonymPrimaryKeyColumns     = []string{"molsyn_id"}
	moleculeSynonymGeneratedColumns      = []string{}
)

type (
	// MoleculeSynonymSlice is an alias for a slice of pointers to MoleculeSynonym.
	// This should almost always be used instead of []MoleculeSynonym.
	MoleculeSynonymSlice []*MoleculeSynonym
	// MoleculeSynonymHook is the signature for custom MoleculeSynonym hook methods
	MoleculeSynonymHook func(context.Context, boil.ContextExecutor, *MoleculeSynonym) error

	moleculeSynonymQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	moleculeSynonymType                 = reflect.TypeOf(&MoleculeSynonym{})
	moleculeSynonymMapping              = queries.MakeStructMapping(moleculeSynonymType)
	moleculeSynonymPrimaryKeyMapping, _ = queries.BindMapping(moleculeSynonymType, moleculeSynonymMapping, moleculeSynonymPrimaryKeyColumns)
	moleculeSynonymInsertCacheMut       sync.RWMutex
	moleculeSynonymInsertCache          = make(map[string]insertCache)
	moleculeSynonymUpdateCacheMut       sync.RWMutex
	moleculeSynonymUpdateCache          = make(map[string]updateCache)
	moleculeSynonymUpsertCacheMut       sync.RWMutex
	moleculeSynonymUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var moleculeSynonymAfterSelectHooks []MoleculeSynonymHook

var moleculeSynonymBeforeInsertHooks []MoleculeSynonymHook
var moleculeSynonymAfterInsertHooks []MoleculeSynonymHook

var moleculeSynonymBeforeUpdateHooks []MoleculeSynonymHook
var moleculeSynonymAfterUpdateHooks []MoleculeSynonymHook

var moleculeSynonymBeforeDeleteHooks []MoleculeSynonymHook
var moleculeSynonymAfterDeleteHooks []MoleculeSynonymHook

var moleculeSynonymBeforeUpsertHooks []MoleculeSynonymHook
var moleculeSynonymAfterUpsertHooks []MoleculeSynonymHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MoleculeSynonym) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range moleculeSynonymAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MoleculeSynonym) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range moleculeSynonymBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MoleculeSynonym) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range moleculeSynonymAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MoleculeSynonym) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range moleculeSynonymBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MoleculeSynonym) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range moleculeSynonymAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MoleculeSynonym) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range moleculeSynonymBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MoleculeSynonym) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range moleculeSynonymAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MoleculeSynonym) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range moleculeSynonymBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MoleculeSynonym) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range moleculeSynonymAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMoleculeSynonymHook registers your hook function for all future operations.
func AddMoleculeSynonymHook(hookPoint boil.HookPoint, moleculeSynonymHook MoleculeSynonymHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		moleculeSynonymAfterSelectHooks = append(moleculeSynonymAfterSelectHooks, moleculeSynonymHook)
	case boil.BeforeInsertHook:
		moleculeSynonymBeforeInsertHooks = append(moleculeSynonymBeforeInsertHooks, moleculeSynonymHook)
	case boil.AfterInsertHook:
		moleculeSynonymAfterInsertHooks = append(moleculeSynonymAfterInsertHooks, moleculeSynonymHook)
	case boil.BeforeUpdateHook:
		moleculeSynonymBeforeUpdateHooks = append(moleculeSynonymBeforeUpdateHooks, moleculeSynonymHook)
	case boil.AfterUpdateHook:
		moleculeSynonymAfterUpdateHooks = append(moleculeSynonymAfterUpdateHooks, moleculeSynonymHook)
	case boil.BeforeDeleteHook:
		moleculeSynonymBeforeDeleteHooks = append(moleculeSynonymBeforeDeleteHooks, moleculeSynonymHook)
	case boil.AfterDeleteHook:
		moleculeSynonymAfterDeleteHooks = append(moleculeSynonymAfterDeleteHooks, moleculeSynonymHook)
	case boil.BeforeUpsertHook:
		moleculeSynonymBeforeUpsertHooks = append(moleculeSynonymBeforeUpsertHooks, moleculeSynonymHook)
	case boil.AfterUpsertHook:
		moleculeSynonymAfterUpsertHooks = append(moleculeSynonymAfterUpsertHooks, moleculeSynonymHook)
	}
}

// One returns a single moleculeSynonym record from the query.
func (q moleculeSynonymQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MoleculeSynonym, error) {
	o := &MoleculeSynonym{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for molecule_synonyms")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MoleculeSynonym records from the query.
func (q moleculeSynonymQuery) All(ctx context.Context, exec boil.ContextExecutor) (MoleculeSynonymSlice, error) {
	var o []*MoleculeSynonym

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MoleculeSynonym slice")
	}

	if len(moleculeSynonymAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MoleculeSynonym records in the query.
func (q moleculeSynonymQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count molecule_synonyms rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q moleculeSynonymQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if molecule_synonyms exists")
	}

	return count > 0, nil
}

// ResStem pointed to by the foreign key.
func (o *MoleculeSynonym) ResStem(mods ...qm.QueryMod) researchStemQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"res_stem_id\" = ?", o.ResStemID),
	}

	queryMods = append(queryMods, mods...)

	return ResearchStems(queryMods...)
}

// MolregnoMoleculeDictionary pointed to by the foreign key.
func (o *MoleculeSynonym) MolregnoMoleculeDictionary(mods ...qm.QueryMod) moleculeDictionaryQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"molregno\" = ?", o.Molregno),
	}

	queryMods = append(queryMods, mods...)

	return MoleculeDictionaries(queryMods...)
}

// LoadResStem allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (moleculeSynonymL) LoadResStem(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMoleculeSynonym interface{}, mods queries.Applicator) error {
	var slice []*MoleculeSynonym
	var object *MoleculeSynonym

	if singular {
		object = maybeMoleculeSynonym.(*MoleculeSynonym)
	} else {
		slice = *maybeMoleculeSynonym.(*[]*MoleculeSynonym)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &moleculeSynonymR{}
		}
		if !queries.IsNil(object.ResStemID) {
			args = append(args, object.ResStemID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &moleculeSynonymR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ResStemID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.ResStemID) {
				args = append(args, obj.ResStemID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`research_stem`),
		qm.WhereIn(`research_stem.res_stem_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ResearchStem")
	}

	var resultSlice []*ResearchStem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ResearchStem")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for research_stem")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for research_stem")
	}

	if len(moleculeSynonymAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ResStem = foreign
		if foreign.R == nil {
			foreign.R = &researchStemR{}
		}
		foreign.R.ResStemMoleculeSynonyms = append(foreign.R.ResStemMoleculeSynonyms, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ResStemID, foreign.ResStemID) {
				local.R.ResStem = foreign
				if foreign.R == nil {
					foreign.R = &researchStemR{}
				}
				foreign.R.ResStemMoleculeSynonyms = append(foreign.R.ResStemMoleculeSynonyms, local)
				break
			}
		}
	}

	return nil
}

// LoadMolregnoMoleculeDictionary allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (moleculeSynonymL) LoadMolregnoMoleculeDictionary(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMoleculeSynonym interface{}, mods queries.Applicator) error {
	var slice []*MoleculeSynonym
	var object *MoleculeSynonym

	if singular {
		object = maybeMoleculeSynonym.(*MoleculeSynonym)
	} else {
		slice = *maybeMoleculeSynonym.(*[]*MoleculeSynonym)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &moleculeSynonymR{}
		}
		args = append(args, object.Molregno)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &moleculeSynonymR{}
			}

			for _, a := range args {
				if a == obj.Molregno {
					continue Outer
				}
			}

			args = append(args, obj.Molregno)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`molecule_dictionary`),
		qm.WhereIn(`molecule_dictionary.molregno in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load MoleculeDictionary")
	}

	var resultSlice []*MoleculeDictionary
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice MoleculeDictionary")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for molecule_dictionary")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for molecule_dictionary")
	}

	if len(moleculeSynonymAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.MolregnoMoleculeDictionary = foreign
		if foreign.R == nil {
			foreign.R = &moleculeDictionaryR{}
		}
		foreign.R.MolregnoMoleculeSynonyms = append(foreign.R.MolregnoMoleculeSynonyms, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Molregno == foreign.Molregno {
				local.R.MolregnoMoleculeDictionary = foreign
				if foreign.R == nil {
					foreign.R = &moleculeDictionaryR{}
				}
				foreign.R.MolregnoMoleculeSynonyms = append(foreign.R.MolregnoMoleculeSynonyms, local)
				break
			}
		}
	}

	return nil
}

// SetResStem of the moleculeSynonym to the related item.
// Sets o.R.ResStem to related.
// Adds o to related.R.ResStemMoleculeSynonyms.
func (o *MoleculeSynonym) SetResStem(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ResearchStem) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"molecule_synonyms\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, []string{"res_stem_id"}),
		strmangle.WhereClause("\"", "\"", 0, moleculeSynonymPrimaryKeyColumns),
	)
	values := []interface{}{related.ResStemID, o.MolsynID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ResStemID, related.ResStemID)
	if o.R == nil {
		o.R = &moleculeSynonymR{
			ResStem: related,
		}
	} else {
		o.R.ResStem = related
	}

	if related.R == nil {
		related.R = &researchStemR{
			ResStemMoleculeSynonyms: MoleculeSynonymSlice{o},
		}
	} else {
		related.R.ResStemMoleculeSynonyms = append(related.R.ResStemMoleculeSynonyms, o)
	}

	return nil
}

// RemoveResStem relationship.
// Sets o.R.ResStem to nil.
// Removes o from all passed in related items' relationships struct.
func (o *MoleculeSynonym) RemoveResStem(ctx context.Context, exec boil.ContextExecutor, related *ResearchStem) error {
	var err error

	queries.SetScanner(&o.ResStemID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("res_stem_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.ResStem = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ResStemMoleculeSynonyms {
		if queries.Equal(o.ResStemID, ri.ResStemID) {
			continue
		}

		ln := len(related.R.ResStemMoleculeSynonyms)
		if ln > 1 && i < ln-1 {
			related.R.ResStemMoleculeSynonyms[i] = related.R.ResStemMoleculeSynonyms[ln-1]
		}
		related.R.ResStemMoleculeSynonyms = related.R.ResStemMoleculeSynonyms[:ln-1]
		break
	}
	return nil
}

// SetMolregnoMoleculeDictionary of the moleculeSynonym to the related item.
// Sets o.R.MolregnoMoleculeDictionary to related.
// Adds o to related.R.MolregnoMoleculeSynonyms.
func (o *MoleculeSynonym) SetMolregnoMoleculeDictionary(ctx context.Context, exec boil.ContextExecutor, insert bool, related *MoleculeDictionary) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"molecule_synonyms\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, []string{"molregno"}),
		strmangle.WhereClause("\"", "\"", 0, moleculeSynonymPrimaryKeyColumns),
	)
	values := []interface{}{related.Molregno, o.MolsynID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Molregno = related.Molregno
	if o.R == nil {
		o.R = &moleculeSynonymR{
			MolregnoMoleculeDictionary: related,
		}
	} else {
		o.R.MolregnoMoleculeDictionary = related
	}

	if related.R == nil {
		related.R = &moleculeDictionaryR{
			MolregnoMoleculeSynonyms: MoleculeSynonymSlice{o},
		}
	} else {
		related.R.MolregnoMoleculeSynonyms = append(related.R.MolregnoMoleculeSynonyms, o)
	}

	return nil
}

// MoleculeSynonyms retrieves all the records using an executor.
func MoleculeSynonyms(mods ...qm.QueryMod) moleculeSynonymQuery {
	mods = append(mods, qm.From("\"molecule_synonyms\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"molecule_synonyms\".*"})
	}

	return moleculeSynonymQuery{q}
}

// FindMoleculeSynonym retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMoleculeSynonym(ctx context.Context, exec boil.ContextExecutor, molsynID int64, selectCols ...string) (*MoleculeSynonym, error) {
	moleculeSynonymObj := &MoleculeSynonym{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"molecule_synonyms\" where \"molsyn_id\"=?", sel,
	)

	q := queries.Raw(query, molsynID)

	err := q.Bind(ctx, exec, moleculeSynonymObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from molecule_synonyms")
	}

	if err = moleculeSynonymObj.doAfterSelectHooks(ctx, exec); err != nil {
		return moleculeSynonymObj, err
	}

	return moleculeSynonymObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MoleculeSynonym) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no molecule_synonyms provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(moleculeSynonymColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	moleculeSynonymInsertCacheMut.RLock()
	cache, cached := moleculeSynonymInsertCache[key]
	moleculeSynonymInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			moleculeSynonymAllColumns,
			moleculeSynonymColumnsWithDefault,
			moleculeSynonymColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(moleculeSynonymType, moleculeSynonymMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(moleculeSynonymType, moleculeSynonymMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"molecule_synonyms\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"molecule_synonyms\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into molecule_synonyms")
	}

	if !cached {
		moleculeSynonymInsertCacheMut.Lock()
		moleculeSynonymInsertCache[key] = cache
		moleculeSynonymInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MoleculeSynonym.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MoleculeSynonym) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	moleculeSynonymUpdateCacheMut.RLock()
	cache, cached := moleculeSynonymUpdateCache[key]
	moleculeSynonymUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			moleculeSynonymAllColumns,
			moleculeSynonymPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update molecule_synonyms, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"molecule_synonyms\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 0, wl),
			strmangle.WhereClause("\"", "\"", 0, moleculeSynonymPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(moleculeSynonymType, moleculeSynonymMapping, append(wl, moleculeSynonymPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update molecule_synonyms row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for molecule_synonyms")
	}

	if !cached {
		moleculeSynonymUpdateCacheMut.Lock()
		moleculeSynonymUpdateCache[key] = cache
		moleculeSynonymUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q moleculeSynonymQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for molecule_synonyms")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for molecule_synonyms")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MoleculeSynonymSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), moleculeSynonymPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"molecule_synonyms\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, moleculeSynonymPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in moleculeSynonym slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all moleculeSynonym")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MoleculeSynonym) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no molecule_synonyms provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(moleculeSynonymColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	moleculeSynonymUpsertCacheMut.RLock()
	cache, cached := moleculeSynonymUpsertCache[key]
	moleculeSynonymUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			moleculeSynonymAllColumns,
			moleculeSynonymColumnsWithDefault,
			moleculeSynonymColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			moleculeSynonymAllColumns,
			moleculeSynonymPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert molecule_synonyms, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(moleculeSynonymPrimaryKeyColumns))
			copy(conflict, moleculeSynonymPrimaryKeyColumns)
		}
		cache.query = buildUpsertQuerySQLite(dialect, "\"molecule_synonyms\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(moleculeSynonymType, moleculeSynonymMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(moleculeSynonymType, moleculeSynonymMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert molecule_synonyms")
	}

	if !cached {
		moleculeSynonymUpsertCacheMut.Lock()
		moleculeSynonymUpsertCache[key] = cache
		moleculeSynonymUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MoleculeSynonym record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MoleculeSynonym) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MoleculeSynonym provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), moleculeSynonymPrimaryKeyMapping)
	sql := "DELETE FROM \"molecule_synonyms\" WHERE \"molsyn_id\"=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from molecule_synonyms")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for molecule_synonyms")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q moleculeSynonymQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no moleculeSynonymQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from molecule_synonyms")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for molecule_synonyms")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MoleculeSynonymSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(moleculeSynonymBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), moleculeSynonymPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"molecule_synonyms\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, moleculeSynonymPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from moleculeSynonym slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for molecule_synonyms")
	}

	if len(moleculeSynonymAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MoleculeSynonym) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMoleculeSynonym(ctx, exec, o.MolsynID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MoleculeSynonymSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MoleculeSynonymSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), moleculeSynonymPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"molecule_synonyms\".* FROM \"molecule_synonyms\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, moleculeSynonymPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MoleculeSynonymSlice")
	}

	*o = slice

	return nil
}

// MoleculeSynonymExists checks if the MoleculeSynonym row exists.
func MoleculeSynonymExists(ctx context.Context, exec boil.ContextExecutor, molsynID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"molecule_synonyms\" where \"molsyn_id\"=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, molsynID)
	}
	row := exec.QueryRowContext(ctx, sql, molsynID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if molecule_synonyms exists")
	}

	return exists, nil
}
